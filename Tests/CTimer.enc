
module CTimer

EMBED
#include <stdio.h>
#include <time.h>
BODY
time_t start_t, end_t;
END

local class CTimer
    -- def start_t() : unit
    --     EMBED (unit) time(&start_t); END
    -- end
    -- def stop_t() : unit
    --     EMBED (unit) time(&end_t); END
    -- end
    -- def print_t() : unit
    --     ()
    -- end
end
