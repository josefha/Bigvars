# bigvar implementation in Encore

### Weekly Goals
  * first prototype
  * spec done

### Notes:
   * future chaining
   * mapreduce framework

### List of functions to impl:
* toarray() -> [t]
* atindex(int) -> t
* atindexes([int]) -> [t]
* applyto(int,fun) -> unit
* map(fun) -> unit
