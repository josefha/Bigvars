
import Bighash


active class Main
    def main() : unit
        var hashtable = new Bighash[String, int]()

        var key1 = "1"
        var key2 = "2"
        var key3 = "3"
        var key4 = "4"
        var key5 = "5"
        var key6 = "6"
        hashtable.put(key1,11111)
        hashtable.put(key2,22222)
        hashtable.put(key3,333333)
        hashtable.put(key4,444444)
        hashtable.put(key5,555555)

        println(hashtable.hasKey(key1))

        -- hashtable.remove(key4)
        -- println(hashtable.contains(333333))
        -- --hashtable.remove(key2)
        --
        -- for element <- hashtable.elements() do
        --     println(element)
        -- end
        --
        -- var p1 = new Person("Josef",21)
        -- var p2 = new Person("Sahand",17)
        -- var p3 = new Person("Liza",17)
        -- var hashtable = new Bighash[int,Person]()
        -- hashtable.put(111,p1)
        -- hashtable.put(222,p2)
        -- hashtable.put(333,p3)
    end
end

read class Person
    val name : String
    val age : int
    def init(name:String, age:int) : unit
        this.name = name
        this.age = age
    end

    def getname() : String
        this.name
    end
end
