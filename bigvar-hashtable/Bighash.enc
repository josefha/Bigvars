

module Bighash

import Supervisorh

passive class Bighash[k,v]
    var supervisor : Supervisor[k,v]

    def init() : unit
        this.supervisor = new Supervisor[k,v]()
    end

    def put(key:k,value:v) : unit
        this.supervisor.put(key,value)
    end

    -- def get(key:K) : V
    --
    -- end

    def remove(key:k) : unit
        print("")
    end

    -- def values() : Bigvar[V]
    --
    -- end
end
