

module Bighash

import Supervisorh

local class Bighash[k,v]
    var supervisor : Supervisor[k,v]

    def init() : unit
        this.supervisor = new Supervisor[k,v]()
    end

    def put(key:k,value:v) : unit
        get(this.supervisor.put(key,value))
    end

    def get(key:k) : v
        this.supervisor.get(key)
    end

    def remove(key:k) : unit
        print("")
    end
end
